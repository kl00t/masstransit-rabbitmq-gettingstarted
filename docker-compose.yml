version: '2'

services:
  rabbitmq:
    image: rabbitmq:3.8-management
    container_name: rabbitmq
    ports:
        - 5672:5672
        - 15672:15672
    volumes:
     - ./volume/rabbitmq:/var/lib/rabbitmq/mnesia
